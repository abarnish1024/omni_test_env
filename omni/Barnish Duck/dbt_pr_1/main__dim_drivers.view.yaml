# Reference this view as main__dim_drivers
#This description was pulled from dbt.
description: One row per driver

schema: dbt_pr_1
table_name: dim_drivers

dimensions:
  driver_ref: {}
  driver_code: {}
  date_of_birth: {}
  nationality: {}

  driver_id:
    format: ID

  driver_name:
    #This description was pulled from dbt.
    description: Display name. Rename this in a dbt PR to show Omni downstream impact.

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: dim_drivers
  target_schema: main
  description: One row per driver
  config:
    materialized: table
  code: |-
    select
      driver_id,
      driver_ref,
      driver_code,
      driver_name,
      date_of_birth,
      nationality
    from {{ ref('stg_drivers') }}
