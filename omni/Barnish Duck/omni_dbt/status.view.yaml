# Reference this view as omni_dbt__status
schema_label: ""

schema: omni_dbt
table_name: status

dimensions:
  status: {}

  status_id:
    sql: '"statusId"'
    format: ID

measures:
  count:
    aggregate_type: count
