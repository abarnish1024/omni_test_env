# Reference this view as main__stg_races
#This description was pulled from dbt.
description: Cleaned race calendar from source f1.races

schema: main
table_name: stg_races

dimensions:
  race_year: {}
  race_round: {}
  race_name: {}
  race_date: {}
  race_url: {}

  race_id:
    format: ID

  circuit_id:
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_races
  target_schema: main
  description: Cleaned race calendar from source f1.races
  config:
    materialized: view
  code: |-
    select
      "raceId" as race_id,
      "year" as race_year,
      "round" as race_round,
      "circuitId" as circuit_id,
      "name" as race_name,
      "date" as race_date,
      "url" as race_url
    from {{ source('f1', 'races') }}
  referenced_by: [ fct_race_results ]
